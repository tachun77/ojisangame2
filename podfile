# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ojisangame' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod "SwiftSpinner"
  # Pods for ojisangame

  target 'ojisangameTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ojisangameUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
